doctype html
html
  head
    title Sửa chức vụ - SPS System
    link(rel="stylesheet", href="/public/style.css")
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css")
  body.dashboard-body
    // Sidebar bên trái
    include sidebar.pug


    // Khu vực bên phải
    main.main-content
      header.topbar
        .search-bar
          i.fas.fa-search
          input(type="text" placeholder="Tìm kiếm nhanh...")
        .user-info
          span.welcome Xin chào, 
            strong #{user.username}
          .avatar #{user.username.charAt(0).toUpperCase()}

      .content-wrapper
        .center-wrapper
          .card-form
            .card-header
              i.fas.fa-edit
              h2(style="margin:0") Sửa thông tin chức vụ
            
            form(method="POST" action=`/positions/edit/${pos._id}`)
              .form-group
                label Mã chức vụ
                input(name="posCode", value=pos.posCode, required)
              
              .form-group
                label Tên chức vụ
                input(name="posName", value=pos.posName, required)
              
              .form-actions
                a.btn-back(href="/positions" style="margin-right: 10px; text-decoration: none; padding: 10px 15px; background: #6c757d; color: white; border-radius: 4px; display: inline-block;") 
                  i.fas.fa-arrow-left(style="margin-right: 5px;")
                  | Hủy bỏ
                button.btn-save(type="submit") Cập nhật

    // Script xử lý đóng/mở mượt mà cho Dropdown Menu
    script.
      document.querySelectorAll('.menu-item.has-submenu').forEach(item => {
        item.addEventListener('click', function(e) {
          e.preventDefault();
          const menuGroup = this.closest('.menu-group');
          const submenu = menuGroup.querySelector('.submenu');
          const toggleIcon = this.querySelector('.toggle-icon');
          
          document.querySelectorAll('.submenu').forEach(sub => {
            if (sub !== submenu) {
              sub.classList.remove('show');
              sub.closest('.menu-group').querySelector('.toggle-icon').classList.remove('rotate');
            }
          });

          submenu.classList.toggle('show');
          toggleIcon.classList.toggle('rotate');
        });
      });